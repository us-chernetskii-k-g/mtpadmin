name: MTPADMIN Stability

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  stability:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Assemble audited runtime
        run: |
          set -euo pipefail
          cat src/mtpadmin.d/00-core.sh src/mtpadmin.d/10-sources.sh src/mtpadmin.d/11-source-stability.sh src/mtpadmin.d/20-admin.sh src/mtpadmin.d/21-admin-tail.sh src/mtpadmin.d/22-guard.sh src/mtpadmin.d/25-doctor-runtime.sh src/mtpadmin.d/29-guard-dispatch.sh src/mtpadmin.d/30-menu.sh > /tmp/mtpadmin
          python3 scripts/normalize_cli.py /tmp/mtpadmin
          cat src/stats_collector.d/00-core.py src/stats_collector.d/10-runtime.py > /tmp/stats.py
          python3 - <<'PY'
          from pathlib import Path
          p=Path('/tmp/stats.py'); s=p.read_text(); m="if __name__=='__main__': raise SystemExit(main() or 0)"
          e=Path('src/stats_collector.d/15-online-history.py').read_text(); assert s.count(m)==1
          p.write_text(s.replace(m,e.rstrip()+'\n\n'+m,1))
          p=Path('/tmp/web.py'); s=''.join(Path('web/mtpadmin_web.d/'+x).read_text() for x in ('00-core.py','10-ui.py','20-pages.py','30-actions.py'))
          m="if __name__=='__main__': main()"; assert s.count(m)==1
          for x in ('35-world-map.py','36-analytics.py','37-analytics-plus.py','38-operations.py','39-compact-ui.py','40-async-update-ui.py','41-webproxy-links-ui.py','42-stability.py','43-db-safety.py','44-webproxy-activity.py','45-ui-loop-fix.py','46-pwa.py'):
              e=Path('web/mtpadmin_web.d/'+x).read_text(); s=s.replace(m,e.rstrip()+'\n\n'+m,1)
          p.write_text(s)
          PY

      - name: Syntax and release contracts
        run: |
          set -euo pipefail
          test "$(cat VERSION)" = '0.12.3'
          grep -q "VERSION='0.12.3'" update.sh
          grep -q "BASE_0122_COMMIT='00fdcb5c44e20cdae932f5eefe95687adcb1860d'" update.sh
          grep -q 'mtpadmin-client-ui' web/mtpadmin_web.d/44-webproxy-activity.py
          grep -q '# MTPADMIN 0.12.3 browser render-loop safety.' web/mtpadmin_web.d/45-ui-loop-fix.py
          grep -q '_CLIENT_SCRIPT = _CLIENT_SCRIPT.replace' web/mtpadmin_web.d/45-ui-loop-fix.py
          grep -q '_PWA_MANIFEST' web/mtpadmin_web.d/46-pwa.py
          bash -n /tmp/mtpadmin update.sh install.sh web-install.sh scripts/webproxy_install.sh scripts/webproxy_backend_install.sh scripts/webproxy_telemetry_install.sh scripts/component_update.sh scripts/scanner_watchdog.sh
          python3 -m py_compile /tmp/web.py /tmp/stats.py src/user_config.py src/scanner_guard.py scripts/update_check.py

      - name: SQLite FD regression
        run: |
          python3 - <<'PY'
          import importlib.util,os,sqlite3,tempfile
          spec=importlib.util.spec_from_file_location('w','/tmp/web.py'); w=importlib.util.module_from_spec(spec); spec.loader.exec_module(w)
          fd,path=tempfile.mkstemp(); os.close(fd)
          c=sqlite3.connect(path); c.execute('create table t(x integer)'); c.execute('insert into t values(1)'); c.commit(); c.close(); w.DB=path
          before=len(os.listdir('/proc/self/fd'))
          for _ in range(500): assert w.scalar('select x from t')==1
          after=len(os.listdir('/proc/self/fd')); assert after-before<=3,(before,after)
          print('FD regression PASS',before,after)
          PY

      - name: Source lifecycle and TOML boundary
        run: |
          python3 - <<'PY'
          import importlib.util,tempfile,tomllib
          from pathlib import Path
          spec=importlib.util.spec_from_file_location('u','src/user_config.py'); u=importlib.util.module_from_spec(spec); spec.loader.exec_module(u)
          p=Path(tempfile.mktemp(suffix='.toml')); p.write_text('[access.users]\n"DEMO"="00112233445566778899aabbccddeeff"\n\n[[upstreams]]\ntype="direct"\nenabled=true\nweight=10\n')
          u.CFG=p
          class A: pass
          a=A(); a.name='DEMO'; a.ad_tag='ffeeddccbbaa99887766554433221100'; a.max_conns='123'; a.max_ips='7'; u.edit(a)
          a.ad_tag='none'; a.max_conns='none'; a.max_ips='none'; u.edit(a)
          b=A(); b.name='AD_01'; b.secret='11'*16; b.ad_tag=None; b.max_conns=None; b.max_ips=None; u.add(b)
          with open(p,'rb') as f:d=tomllib.load(f)
          assert d['access']['users']['AD_01']==b.secret and 'AD_01' not in d['upstreams'][0]
          p.write_text('[access.users]\n"MAIN"="00112233445566778899aabbccddeeff"\n\n[[upstreams]]\ntype="direct"\n"WEB_PROXY"="22222222222222222222222222222222"\n')
          u.CFG=p; u.repair_misplaced(A())
          with open(p,'rb') as f:d=tomllib.load(f)
          assert d['access']['users']['WEB_PROXY']=='22222222222222222222222222222222' and 'WEB_PROXY' not in d['upstreams'][0]
          print('Source lifecycle PASS')
          PY

      - name: Responsive routes PWA and render-loop safety
        run: |
          python3 - <<'PY'
          import importlib.util,json
          spec=importlib.util.spec_from_file_location('w','/tmp/web.py'); w=importlib.util.module_from_spec(spec); spec.loader.exec_module(w)
          w.VERSION='0.12.3'
          w.state=lambda:{'PROFILE':'A','WEBPROXY_SOURCE':'WEB_PROXY','WEBPROXY_HOST':'webproxy.example.com','WEBPROXY_READY':'1','MTPADMIN_VERSION':'0.12.3'}
          w.query=lambda *a,**k:[]; w.scalar=lambda *a,**k:0
          w._o_service=lambda *a:True; w._o_http_ok=lambda *a:True; w._o_health=lambda:(100,[['TeleMT','PASS']])
          w._o_update_status=lambda:{'components':{k:{'current':'1','latest':'1','available':False} for k in ('mtpadmin','telemt','webproxy')},'checked_at':1,'updates':0}
          w._o_component_status=lambda:{}; w._o_backups=lambda:'backup'; w._o_learning=lambda:'guard'; w._o_webproxy_link=lambda:''; w._o_qr=lambda x:''
          w._wpa_source_rows=lambda:[{'username':'A','current_connections':4,'active_unique_ips':1},{'username':'WEB_PROXY','current_connections':0,'active_unique_ips':0}]
          w._wpa_metrics=lambda:{'tproxy_sessions_live':3,'tproxy_streams_live':8}; w._wpa_clients=lambda:[{'ip':'198.51.100.10','sessions':2},{'ip':'2001:db8::10','sessions':1}]
          w.api_json=lambda p,**k:[{'username':'A','active_ips':['203.0.113.7']}] if p=='/v1/stats/users/active-ips' else []
          helper=w.active_clients_html(); assert '198.51.100.10' in helper and '2001:db8::10' in helper
          shell=w.page_template('Активные',helper,'admin','active')
          dangerous='new MutationObserver(enhance).observe(document.documentElement,{childList:true,subtree:true});'
          assert 'mtp-modern' in shell and 'MTPADMIN 0.12.3: self-triggering MutationObserver disabled' in shell and dangerous not in shell
          assert 'manifest.webmanifest' in shell and 'm-pwa-sheet' in shell and 'Продолжить в браузере' in shell
          manifest=json.loads(w._PWA_MANIFEST.decode())
          assert manifest['display']=='standalone' and manifest['scope']=='/'
          assert w._pwa_icon_png(192).startswith(b'\x89PNG') and w._pwa_icon_png(512).startswith(b'\x89PNG')
          assert 'caches.' not in w._PWA_SW.decode()
          sent=[]; w._x_online_card=lambda *a,**k:'ONLINE'
          class H:
              path='/active?range=6h'
              def require_user(self): return True
              def params(self): return {'range':'6h'}
              def send_html(self,*a,**k): sent.append((a,k))
          w._x_do_GET(H()); route=sent[-1][0][1]
          assert '198.51.100.10' in route and 'WEB потоки' in route
          print('Responsive real-route + PWA PASS')
          PY

      - name: Update and public-link identity contracts
        run: |
          set -euo pipefail
          MTPADMIN_WEBPROXY_PROBE_RUNTIME_JSON='{"data":[{"username":"WEB_PROXY","in_runtime":true}]}' MTPADMIN_WEBPROXY_PROBE_SOURCE=WEB_PROXY bash scripts/webproxy_install.sh
          if MTPADMIN_WEBPROXY_PROBE_RUNTIME_JSON='{"data":[{"username":"WEB_PROXY","in_runtime":false}]}' MTPADMIN_WEBPROXY_PROBE_SOURCE=WEB_PROXY bash scripts/webproxy_install.sh; then exit 1; fi
          grep -q 'WEB_IDENTITY_BEFORE' update.sh
          grep -q 'MTPADMIN 0.12.3: self-triggering MutationObserver disabled' update.sh
          grep -q 'PWA потерян после стабилизации интерфейса' update.sh
          grep -q '_a_active_clients_html = active_clients_html' web/mtpadmin_web.d/44-webproxy-activity.py
          grep -q 'post-update validation/repair' scripts/component_update.sh

      - name: Production safety contracts
        run: |
          set -euo pipefail
          grep -q 'source_apply_or_restart' src/mtpadmin.d/11-source-stability.sh
          grep -q '"max_pending_items_per_session": 16384' scripts/webproxy_install.sh
          grep -q 'restore_patched_backup' scripts/webproxy_telemetry_install.sh
          grep -q 'GOTMPDIR="$gotmp"' scripts/webproxy_telemetry_install.sh
          grep -q 'MAX_AGE=.*120' scripts/scanner_watchdog.sh
          grep -q 'systemctl restart "$SCANNER"' scripts/scanner_watchdog.sh

      - name: Product and secret guard
        run: |
          set -euo pipefail
          grep -q 'свой Telegram Proxy с веб' README.md
          grep -q 'Новый Telegram WEB Proxy' README.md
          grep -q 'https://t.me/boss_of_this_vpn' README.md
          python3 - <<'PY'
          import pathlib,re
          allowed={'00112233445566778899aabbccddeeff','ffeeddccbbaa99887766554433221100','11111111111111111111111111111111','22222222222222222222222222222222','33333333333333333333333333333333'}
          bad=[]
          for p in pathlib.Path('.').rglob('*'):
              if not p.is_file() or '.git' in p.parts or p.suffix.lower() in {'.png','.jpg','.jpeg','.gif','.webp','.zip','.gz','.db'}: continue
              try:s=p.read_text(errors='ignore')
              except Exception:continue
              for m in re.finditer(r'(?i)(?:secret|password|token)\s*[=:]\s*["\x27]?([0-9a-f]{32,}|[A-Za-z0-9_-]{40,})',s):
                  if m.group(1).lower() not in allowed: bad.append((str(p),m.group(1)[:12]))
          assert not bad,bad
          print('Secret scan PASS')
          PY
